# we can define here what version of the package we want
# its important so every users will have same lib versions

hunter_config(Boost VERSION 1.71.0-p0)
hunter_config(ZLIB VERSION 1.2.11-p1)
hunter_config(BZip2 VERSION 1.0.8-p0)
hunter_config(MySQL-client VERSION 6.1.9-p1)

